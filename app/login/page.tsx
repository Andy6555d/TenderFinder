import Link from 'next/link'; import { login } from '@/app/actions'; import SubmitButton from '@/components/SubmitButton'
export default function Page({searchParams}:{searchParams:{error?:string}}){return <div className="wrap"><div className="auth-shell"><h1>Member login</h1><p className="sub">United Hardware member access.</p>{searchParams.error&&<div className="error-box">{searchParams.error}</div>}<form action={login}><div className="field"><label>Email</label><input name="email" type="email" required/></div><div className="field"><label>Password</label><input name="password" type="password" required/></div><SubmitButton className="btn btn-primary btn-full" pendingLabel="Logging in…">Log in</SubmitButton></form><div className="auth-switch">Need access? <Link href="/signup">Request account</Link></div></div></div>}
