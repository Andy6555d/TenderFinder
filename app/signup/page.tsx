import Link from 'next/link'; import { signup } from '@/app/actions'
export default function Page({searchParams}:{searchParams:{error?:string}}){return <div className="wrap"><div className="auth-shell"><h1>Request member access</h1><p className="sub">Accounts are free for approved member outlets. An administrator must approve access.</p>{searchParams.error&&<div className="error-box">{searchParams.error}</div>}<form action={signup}><div className="field"><label>Outlet / company *</label><input name="outlet_name" required/></div><div className="field"><label>Your name</label><input name="contact_name"/></div><div className="field"><label>Email *</label><input name="email" type="email" required/></div><div className="field"><label>Password *</label><input name="password" type="password" minLength={8} required/></div><button className="btn btn-primary btn-full">Request access</button></form><div className="auth-switch">Already approved? <Link href="/login">Log in</Link></div></div></div>}
