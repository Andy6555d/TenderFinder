import { createClient } from '@/lib/supabase/server'; import { redirect } from 'next/navigation'; import { logout } from '@/app/actions'
export default async function Page(){const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const {data:p}=await s.from('profiles').select('status,outlet_name').eq('id',user.id).maybeSingle();if(p?.status==='approved')redirect('/dashboard');return <div className="wrap"><div className="notice-page"><div className="status-icon">✓</div><h1>Access request received</h1><p>Your account for <strong>{p?.outlet_name||'your outlet'}</strong> is waiting for administrator approval.</p><p className="muted">Once approved you can see filtered supply-only tender opportunities and create private pricing sheets.</p><form action={logout}><button className="btn btn-secondary">Log out</button></form></div></div>}
