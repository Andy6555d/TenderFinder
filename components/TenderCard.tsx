import Link from 'next/link'; import type { Tender } from '@/lib/types'
function euro(v:number|null){return v==null?'Value not stated':new Intl.NumberFormat('en-IE',{style:'currency',currency:'EUR',maximumFractionDigits:0}).format(v)}
function date(v:string|null){return v?new Intl.DateTimeFormat('en-IE',{dateStyle:'medium',timeStyle:'short'}).format(new Date(v)):'Not stated'}
export default function TenderCard({t}:{t:Tender}){return <article className="tender-card"><div className="score"><span>{t.relevance_score}</span><small>match</small></div><div className="tender-main"><div className="tender-top"><span className="badge green">SUPPLY ONLY</span>{t.categories?.slice(0,3).map(c=><span className="badge" key={c}>{c}</span>)}</div><h2><Link href={`/tenders/${t.id}`}>{t.title}</Link></h2><p className="authority">{t.authority||'Contracting authority not parsed'}</p><p className="desc">{t.description?.slice(0,240)||'Open the opportunity for full public notice details.'}</p><div className="facts"><span><b>Deadline</b>{date(t.deadline_at)}</span><span><b>Estimated value</b>{euro(t.estimated_value)}</span><span><b>Procedure</b>{t.procedure||'—'}</span></div></div></article>}
